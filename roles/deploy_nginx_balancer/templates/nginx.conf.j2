user www-data;
worker_processes 1;

events {
    worker_connections 1024;
}
http {
upstream localhost{
    server app1:5000; 
    server app2:5000;
    server app3:5000;
	}
server {
    listen "{{ nginx_listen_port }}";
    location / {
    proxy_pass http://localhost; 
 } 
}

}
